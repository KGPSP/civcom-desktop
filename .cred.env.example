# Local-only template for the future manual, interactive production check.
# Copy to .cred.env, set mode 0600, and never commit or print its values.
# Production code and CI must not load this file.
CIVCOM_TEST_USERNAME=replace-with-local-test-account
CIVCOM_TEST_PASSWORD=replace-with-local-test-password
